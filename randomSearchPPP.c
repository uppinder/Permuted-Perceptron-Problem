#include <stdio.h>
#include <stdlib.h>

int main(int argc,char* argv[]) {

	/*
		Take random e-matrix A(mxn) and a non-negative
		vector S(mx1). Generate random e-vectors X(nx1).
		Check if AX = S. If yes, report X else change one 
		element of X.

	 */

	return 0;
}